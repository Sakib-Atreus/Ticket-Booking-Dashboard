import {
    createBrowserRouter,
  } from "react-router-dom";

const router = createBrowserRouter([
    {
      path: "/",
      element: <div>Welcome to the ticket booking dashboard!</div>,
    },
  ]);

  export default router;